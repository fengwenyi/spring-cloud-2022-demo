server:
  port: 9999

spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yml
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: '*'